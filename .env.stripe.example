# ============================================================================
# STRIPE CONFIGURATION
# ============================================================================
# Copy this to your .env file and fill in your Stripe credentials

# Stripe API Keys
# Get these from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_your_secret_key_here
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here

# Stripe Webhook Secret
# For local development: Run `stripe listen --forward-to localhost:3000/api/stripe/webhooks`
# For production: Create webhook endpoint in Stripe Dashboard
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Stripe Products & Prices
# Run `npm run setup-stripe-products` to create these and get the IDs
STRIPE_PRODUCT_PASS_MONTHLY=prod_xxxxxxxxxxxxx
STRIPE_PRODUCT_PASS_ANNUAL=prod_xxxxxxxxxxxxx
STRIPE_PRICE_PASS_MONTHLY=price_xxxxxxxxxxxxx
STRIPE_PRICE_PASS_ANNUAL=price_xxxxxxxxxxxxx

# Application URL
# Used for Stripe redirect URLs (success/cancel pages)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================================================
# SETUP INSTRUCTIONS
# ============================================================================
#
# 1. Create a Stripe account at https://stripe.com
#
# 2. Get your API keys from the Dashboard
#
# 3. Run the setup script to create products:
#    npm run setup-stripe-products
#
# 4. Copy the product and price IDs from the output
#
# 5. Set up webhooks:
#    - Local: stripe listen --forward-to localhost:3000/api/stripe/webhooks
#    - Production: Add endpoint in Stripe Dashboard
#
# 6. Run database migration:
#    npx prisma migrate dev --name add-stripe-integration
#
# 7. Start your application and test!
#
# For detailed instructions, see docs/stripe-integration.md
#
